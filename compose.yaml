services:
  mira-notification-router:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      MIRA_DEPLOY_PROFILE: notification-router
      HOST: 0.0.0.0
      PORT: 3302
    ports:
      - "3302:3302"
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - fetch('http://127.0.0.1:3302/v1/health').then((response)=>process.exit(response.ok?0:1)).catch(()=>process.exit(1))
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 5s
